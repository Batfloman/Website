export default class LineStyle {
    constructor(borderColor, fillColor, lineWidth) {
        this.borderColor = borderColor;
        this.fillColor = fillColor;
        this.lineWidth = lineWidth;
    }
}
